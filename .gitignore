# generated output / large files (never commit)
output/
anims/
checkpoints/
*.nc
*.jld2
checkpoint_*
logs/*.out
logs/*.log
*.mp4
*.gif

# resolved Julia manifest: let each machine resolve fresh (laptop vs cluster Julia differ).
# The tested 1.10.11 manifest lives locally but is not committed; setup_casper.sh resolves
# a fresh one on the cluster (Project.toml pins Oceananigans 0.109 / Oceanostics 0.16).
Manifest.toml
Manifest.*.toml

# python / macOS cruft
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
